# Img-Recognation-TensorFlow
Simple CLI app made with TensorFlow

#### To build my project ypu must write:
  ```shell
sudo docker build -t imgrecognition.
```
#### After this, write:
  ```shell
sudo docker run imgrecognition IMAGE_URL_THERE
```
